---
phase: 02-communication-patterns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - COMM-01

must_haves:
  truths:
    - "Services discover each other via Docker DNS (service-a:8001, service-b:8002)"
    - "API Gateway routes /api/users/* to service-a and /api/orders/* to service-b"
    - "Health endpoints return 200 when services are ready"
    - "docker-compose up starts all services without manual configuration"
  artifacts:
    - path: "basic/projects/microservices/docker-compose.yml"
      provides: "Full stack orchestration with Docker DNS"
    - path: "basic/projects/microservices/cmd/gateway/main.go"
      provides: "API Gateway entry point with chi router"
    - path: "basic/projects/microservices/cmd/service-a/main.go"
      provides: "User service entry point"
    - path: "basic/projects/microservices/cmd/service-b/main.go"
      provides: "Order service entry point"
    - path: "basic/projects/microservices/internal/gateway/proxy.go"
      provides: "HTTP proxy logic to backend services"
    - path: "basic/projects/microservices/.github/workflows/ci.yml"
      provides: "CI pipeline for microservices"
  key_links:
    - from: "docker-compose.yml"
      to: "cmd/gateway, cmd/service-a, cmd/service-b"
      via: "build context per service"
    - from: "gateway"
      to: "service-a:8001"
      via: "SERVICE_A_URL env var (Docker DNS)"
    - from: "service-b"
      to: "service-a:8001"
      via: "USER_SERVICE_URL env var (Docker DNS)"
---

<objective>
Create a microservices project template with Docker Compose service discovery for the Go Pro Learning Platform.

Purpose: Teaches Docker-based service orchestration, inter-service communication via HTTP, and API Gateway pattern.
Output: Runnable `basic/projects/microservices/` template with gateway, user service, order service, Docker Compose, and CI.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-communication-patterns/02-RESEARCH.md
@.planning/phases/02-communication-patterns/02-CONTEXT.md

# Docker Compose DNS Pattern (from advanced-topics/06-microservices-docker)
```yaml
# Service discovery via Docker DNS - services reference each other by name
services:
  service-a:
    environment:
      - SERVICE_PORT=8001
    networks:
      - microservices
  
  service-b:
    environment:
      - USER_SERVICE_URL=http://service-a:8001  # Docker DNS
    depends_on:
      service-a:
        condition: service_healthy
    networks:
      - microservices

networks:
  microservices:
    driver: bridge
```

# Project Structure
```
basic/projects/microservices/
├── docker-compose.yml              # Full stack with health checks
├── cmd/
│   ├── gateway/main.go             # API Gateway (chi router)
│   ├── service-a/main.go           # User service
│   └── service-b/main.go           # Order service
├── internal/
│   ├── gateway/
│   │   ├── proxy.go               # Reverse proxy to services
│   │   ├── registry.go            # Service endpoint registry
│   │   └── routes.go              # Route configuration
│   ├── service-a/
│   │   ├── handler.go             # User handlers
│   │   └── models.go              # User/Order types
│   └── service-b/
│       ├── handler.go             # Order handlers
│       └── models.go              # Order types
├── Dockerfile                      # Multi-stage for each service
├── Makefile
├── go.mod (go 1.23)
└── README.md
```

# Locked Decisions (from 02-CONTEXT.md)
| Pattern | Decision |
|---------|----------|
| Docker Compose DNS | ✅ SELECTED - Service discovery via Docker networking |
| API Gateway | ✅ SELECTED - gin-based gateway with proxy routes |
| Health checks | ✅ SELECTED - Docker healthcheck + /health endpoints |
| Environment variables | ✅ SELECTED - Configuration via env vars |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create microservices project structure with Docker Compose</name>
  <files>basic/projects/microservices/go.mod,basic/projects/microservices/docker-compose.yml,basic/projects/microservices/cmd/service-a/main.go,basic/projects/microservices/cmd/service-b/main.go</files>
  <action>
    Create the complete microservices project structure:

    1. **go.mod** - Module `basic/projects/microservices`, Go 1.23

    2. **docker-compose.yml** - Full stack orchestration:
       - PostgreSQL 15-alpine for service-a (users-db on 5433)
       - PostgreSQL 15-alpine for service-b (orders-db on 5434)
       - Redis 7-alpine for caching (6379)
       - service-a: port 8001, depends on users-db + redis
       - service-b: port 8002, depends on orders-db + service-a
       - api-gateway: port 8080, depends on service-a + service-b
       - All services on `microservices` bridge network
       - Health checks: wget --spider on /health endpoint

    3. **cmd/service-a/main.go** - User service:
       - Listen on SERVICE_PORT env var (default 8001)
       - Endpoints: GET /health, GET /api/users, GET /api/users/{id}
       - In-memory user store (2-3 seed users)
       - Return proper HTTP status codes

    4. **cmd/service-b/main.go** - Order service:
       - Listen on SERVICE_PORT env var (default 8002)
       - Endpoints: GET /health, GET /api/orders, GET /api/orders/{id}
       - USER_SERVICE_URL env var to call user service
       - In-memory order store (linked to users)
       - Return proper HTTP status codes

    Per D-02 (Docker Compose DNS): Use service names (service-a, service-b) for discovery.
  </action>
  <verify>
    <automated>cd basic/projects/microservices && docker-compose config --quiet && echo "Docker Compose valid"</automated>
  </verify>
  <done>Project structure exists, docker-compose.yml validates, services defined</done>
</task>

<task type="auto">
  <name>Task 2: Create API Gateway with proxy routing</name>
  <files>basic/projects/microservices/cmd/gateway/main.go,basic/projects/microservices/internal/gateway/proxy.go,basic/projects/microservices/internal/gateway/routes.go,basic/projects/microservices/internal/gateway/registry.go</files>
  <action>
    Create the API Gateway with chi router and proxy routing:

    1. **cmd/gateway/main.go** - Gateway entry point:
       - Listen on GATEWAY_PORT env var (default 8080)
       - Use chi v5 router (consistent with Phase 1)
       - Register /health, /api/users/*, /api/orders/* routes
       - Wire proxy and registry

    2. **internal/gateway/registry.go** - Service endpoint registry:
       ```go
       type Registry struct {
           userServiceURL  string  // From USER_SERVICE_URL env
           orderServiceURL string  // From ORDER_SERVICE_URL env
       }
       ```
       - Reads URLs from environment variables
       - Provides GetUserServiceURL(), GetOrderServiceURL() methods

    3. **internal/gateway/proxy.go** - HTTP reverse proxy:
       - Creates httputil.ReverseProxy for each backend
       - Rewrites /api/users/* -> /api/users/* on user service
       - Rewrites /api/orders/* -> /api/orders/* on order service
       - Director function modifies request URL

    4. **internal/gateway/routes.go** - Route configuration:
       - Mounts proxy handlers on chi router
       - GET /health returns `{"status": "ok", "service": "gateway"}`

    Per D-03 (API Gateway pattern): Gateway uses chi v5 router consistent with Phase 1.
  </action>
  <verify>
    <automated>cd basic/projects/microservices && go build ./cmd/gateway/... && echo "Gateway builds"</automated>
  </verify>
  <done>Gateway proxies requests to services, routes configured correctly</done>
</task>

<task type="auto">
  <name>Task 3: Add Docker, CI pipeline, and documentation</name>
  <files>basic/projects/microservices/Dockerfile,basic/projects/microservices/.github/workflows/ci.yml,basic/projects/microservices/README.md,basic/projects/microservices/Makefile</files>
  <action>
    Add production infrastructure:

    1. **Dockerfile** - Multi-stage build for each service:
       ```dockerfile
       FROM golang:1.23-alpine AS builder
       WORKDIR /app
       COPY go.mod go.sum ./
       RUN go mod download
       COPY . .
       # Build specific service
       RUN go build -o service-a ./cmd/service-a

       FROM alpine:latest
       COPY --from=builder /app/service-a /service-a
       EXPOSE 8001
       CMD ["/service-a"]
       ```
       - Create similar stages for service-b and gateway
       - Use docker-compose build context per service

    2. **.github/workflows/ci.yml** - GitHub Actions:
       - Go 1.23 setup
       - Build all services: `go build ./cmd/...`
       - Test: `go test ./... -short`
       - Docker Compose validation
       - Note: Full docker-compose up requires separate job

    3. **README.md** - Documentation:
       - Architecture diagram (ASCII)
       - Service discovery explanation
       - Local run: `docker-compose up`
       - Test endpoints: `curl localhost:8080/api/users`
       - Health check: `curl localhost:8080/health`

    4. **Makefile** - Common commands:
       - `make run` (docker-compose up)
       - `make build` (docker-compose build)
       - `make test` (go test ./...)
       - `make clean` (docker-compose down)
  </action>
  <verify>
    <automated>cd basic/projects/microservices && docker build -t microservices-gateway -f Dockerfile . 2>&1 | tail -3</automated>
  </verify>
  <done>Docker builds successfully, CI pipeline configured, README complete</done>
</task>

</tasks>

<verification>
1. `docker-compose config` passes without errors
2. `go build ./cmd/...` passes for all services
3. Gateway serves /health and proxies to /api/users/* and /api/orders/*
4. Services use Docker DNS names for inter-service communication
</verification>

<success_criteria>
- `docker-compose up -d` starts all 6 containers (3 services + 3 databases/redis)
- `curl http://localhost:8080/health` returns gateway health
- `curl http://localhost:8080/api/users` returns users from service-a
- `curl http://localhost:8001/health` returns service-a health directly
- All services can communicate via Docker DNS names
</success_criteria>

<output>
After completion, create `.planning/phases/02-communication-patterns/02-01-SUMMARY.md`
</output>
