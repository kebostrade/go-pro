# Quiz: Few-Shot Learning

## Question 1
What is few-shot prompting?

A) Training a model with very small datasets
B) Providing 2-5 examples in the prompt to guide the model's output
C) Using the smallest possible prompts
D) Testing prompts with minimal model capabilities

## Question 2
What is the ideal number of examples for most few-shot tasks?

A) 1
B) 2-5
C) 10-20
D) As many as possible

## Question 3
When selecting examples for few-shot prompting, you should:

A) Use random examples
B) Use examples that are representative and cover the desired output patterns
C) Use the longest examples possible
D) Use examples without any pattern

## Question 4
What happens if your few-shot examples are inconsistent in format?

A) Nothing, format doesn't matter
B) The model may produce inconsistent outputs matching the mixed formats
C) The model will automatically correct them
D) The model will ignore them

## Question 5
What is a key advantage of few-shot over zero-shot prompting?

A) It uses less tokens
B) It provides more context and guidance for the desired output format
C) It works with any model
D) It doesn't require any examples

## Question 6
Which is NOT a good practice for few-shot examples?

A) Use diverse examples covering edge cases
B) Keep examples consistent in format
C) Use very long, detailed examples
D) Include both positive and negative examples when helpful

## Question 7
What is "k-shot" prompting where k is a number?

A) A prompting technique using k number of examples
B) A method to kill the model process
C) A security feature
D) A type of model architecture

## Question 8
When should you use fewer examples (1-2) instead of more (5+)?

A) When the task is very simple and format is obvious
B) When you want the model to be more creative
C) When using older models
D) Never, more examples are always better

## Question 9
What is a limitation of few-shot prompting?

A) It only works with GPT models
B) Examples consume tokens that could be used for context
C) It cannot handle complex tasks
D) It requires internet access

## Question 10
What is the relationship between few-shot learning and in-context learning?

A) They are unrelated
B) Few-shot is a form of in-context learning where examples are provided in the context
C) In-context learning is a subset of few-shot
D) Few-shot requires training, in-context doesn't
